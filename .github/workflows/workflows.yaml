name: github-action-ci

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    name: first job
    steps:
      - name: echo command
      - uses: actions/checkout@master
        env:
          MY_VAR: variable
        run: |
          echo MY_VAR
